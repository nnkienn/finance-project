# ==========================
# 🚀 Global Environment Example
# ==========================

# ==== Kafka (Confluent / KRaft) ====
# ⚠️ Đây chỉ là ví dụ, cluster ID bạn có thể generate lại bằng lệnh:
# docker exec -it kafka kafka-storage random-uuid
KAFKA_CLUSTER_ID=your-cluster-id-here

# ==== Kafka Config (for Backend) ====
SPRING_KAFKA_BOOTSTRAP_SERVERS=kafka:9092
SPRING_KAFKA_CONSUMER_GROUP_ID=finance-app
SPRING_KAFKA_CONSUMER_AUTO_OFFSET_RESET=earliest
SPRING_KAFKA_PRODUCER_ACKS=all
SPRING_KAFKA_PRODUCER_RETRIES=5
SPRING_KAFKA_PRODUCER_IDEMPOTENCE=true
SPRING_KAFKA_PRODUCER_LINGER_MS=5

# ==== Backend Config ====
BACKEND_PORT=8080
SPRING_PROFILES_ACTIVE=gmail

# ==== Frontend Config ====
FRONTEND_PORT=3000

# ==== (Optional) Network Name for Docker Compose ====
# Nếu bạn có nhiều compose, đặt tên network để tránh xung đột
DOCKER_NETWORK=finance_net
